apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: labs-tls
  namespace: envoy-gateway-system
spec:
  dnsNames:
    - labs.montevive.ai
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: labs-tls
